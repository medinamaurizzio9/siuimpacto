<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;font-size:12px}.header{border-bottom:2px solid {{ $settings['primary_color'] ?? '#111' }};margin-bottom:18px;padding-bottom:10px}.title{font-size:18px;font-weight:bold}.logo{max-height:70px}.table{width:100%;border-collapse:collapse}.table th,.table td{border:1px solid #ccc;padding:6px;text-align:left}.footer{border-top:1px solid #ddd;margin-top:20px;padding-top:8px;text-align:center;color:#555}</style></head><body>
@php($logo = $settings['logo_pdf'] ?: ($settings['logo_main'] ?? null))
<div class="header">@if($logo)<img class="logo" src="{{ public_path('storage/'.$logo) }}" alt="Logo">@endif<div class="title">{{ $settings['system_name'] ?? 'IMPACTO URBANIZACIONES' }} - {{ $settings['system_subtitle'] ?? 'Sistema Integral de Terrenos' }}</div><p>Plan de pagos</p></div>
@php($saldoTotal = $venta->cuotas->sum('saldo_pendiente'))
<p><strong>Cliente:</strong> {{ $venta->cliente->nombre }}<br><strong>Lote:</strong> {{ $venta->lote->manzano->codigo }}-{{ $venta->lote->codigo }}<br><strong>Precio:</strong> {{ number_format($venta->precio_final, 2) }}<br><strong>Anticipo:</strong> {{ number_format($venta->cuota_inicial, 2) }}<br><strong>Saldo total:</strong> {{ number_format($saldoTotal, 2) }}</p>
<table class="table"><thead><tr><th>#</th><th>Fecha</th><th>Monto</th><th>Pagado</th><th>Saldo</th><th>Estado</th></tr></thead><tbody>@foreach($venta->cuotas as $cuota)<tr><td>{{ $cuota->numero }}</td><td>{{ optional($cuota->fecha_programada)->format('d/m/Y') }}</td><td>{{ number_format($cuota->monto, 2) }}</td><td>{{ number_format($cuota->monto_pagado, 2) }}</td><td>{{ number_format($cuota->saldo_pendiente, 2) }}</td><td>{{ $cuota->estado }}</td></tr>@endforeach<tr><td colspan="4"><strong>Saldo total</strong></td><td><strong>{{ number_format($saldoTotal, 2) }}</strong></td><td></td></tr></tbody></table>
<div class="footer">{{ $settings['footer_text'] ?? '' }}</div>
</body></html>
